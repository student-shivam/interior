# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/interior-contact

# Gmail SMTP credentials (use an app password for Gmail accounts)
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password

# Email address that receives the inquiry notifications
CONTACT_RECEIVER=your-email@gmail.com

# Server port
PORT=5000
